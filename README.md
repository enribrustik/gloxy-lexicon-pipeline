# gloxy-lexicon-pipeline
Extracts subject-specific vocabulary from per-subject corpora using spaCy lemmatisation and TF-IDF, then rates each term's difficulty on two axes: where it appears across university-year corpora, and how common it is in general Italian (Zipf frequency). Built to replace the AI-approximated word pool behind Gloxy, an educational quiz game.
